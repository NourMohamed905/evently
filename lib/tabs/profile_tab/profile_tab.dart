import 'package:flutter/material.dart';

class profileTab extends StatelessWidget {
  const profileTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('profile Tab')));
  }
}
